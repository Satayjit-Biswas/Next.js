import React from "react";

const Recent_post = () => {
    return (
        <div className={recentpost_date}>
            <div className="recentpost_date">{blog_date}</div>
        </div>
    );
};

export default Recent_post;
